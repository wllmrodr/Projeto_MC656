version: '3'

services:
  react-app:
    container_name: react-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    volumes:
      - './src:/app/src'
      - '/app/node_modules'
    stdin_open: true
    tty: true
